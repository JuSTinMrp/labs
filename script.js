// document.addEventListener("DOMContentLoaded", function () {
//     function getRandomPosition() {
//         const x = Math.random() * (window.innerWidth - 100); // Adjusted for image size (100px)
//         const y = Math.random() * (window.innerHeight - 100); // Adjusted for image size (100px)
//         return { x, y };
//     }

//     for (let i = 1; i <= 10; i++) {
//         const img = document.getElementById(`img${i}`);
//         const { x, y } = getRandomPosition();
//         img.style.top = `${y}px`;
//         img.style.left = `${x}px`;
//         img.classList.add('random-position');
//     }
// });
